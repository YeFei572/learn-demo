syntax = "proto3";
package proto;
// optimize_for 加快解析速度
option optimize_for = SPEED;
option java_package = "com.dikar.common.protobuf";

message WSBaseReqProto {
  int32 type = 1;
  uint64 uid = 2; // 用户ID
  string sid = 3;  // 登陆验证
}
